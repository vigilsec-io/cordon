# Vigil config for the vigil project itself.
# Exclude the rules/ directory — rule name strings match the injection/secret
# patterns Vigil detects, causing false positives on the rule definitions.
exclude_paths = ["rules", "fixtures"]
